import React from 'react';
import {
  View,
} from 'react-native';
import SearchBar from './ src/components/SearchBar';

const App: React.FC = () => (
  <View>
    <SearchBar value="" onChangeValue={() => {}} />
  </View>
);

export default App;
