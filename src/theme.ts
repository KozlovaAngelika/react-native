import { COLORS } from './utils/constants';

const theme = {
  Button: {
    titleStyle: {
      color: COLORS.WHITE,
    },
    buttonStyle: {
      backgroundColor: COLORS.GREY,
      borderRadius: 10,
    },
  },
  Icon: {
    color: COLORS.GREY,
    size: 25,
  },
};

export default theme;
