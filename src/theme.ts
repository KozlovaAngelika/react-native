import { COLORS } from './utils/constants';

const theme = {
  Button: {
    titleStyle: {
      color: COLORS.WHITE,
    },
    buttonStyle: {
      backgroundColor: COLORS.LIGHT_GREY,
      borderRadius: 10,
    },
  },
};

export default theme;
